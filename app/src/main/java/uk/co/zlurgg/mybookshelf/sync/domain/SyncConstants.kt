package uk.co.zlurgg.mybookshelf.sync.domain

/**
 * Domain-level constants for sync operations.
 */
object SyncConstants {
    /**
     * Timber log tag for sync trigger events.
     * Used when data mutations trigger immediate sync.
     */
    const val TAG_SYNC_TRIGGER = "SyncTrigger"
}
