<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG-ONLY network security configuration.
    Allows cleartext HTTP to Firebase emulators for local development.
    This file ONLY applies to debug builds - release builds use the main config.
-->
<network-security-config>
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Domain-specific configurations for APIs -->
    <domain-config cleartextTrafficPermitted="false">
        <!-- Open Library API - HTTPS only -->
        <domain includeSubdomains="true">openlibrary.org</domain>

        <!-- GitHub Pages (share links) - HTTPS only -->
        <domain includeSubdomains="true">zlurgg.github.io</domain>
    </domain-config>

    <!-- Firebase Emulator - Allow cleartext HTTP for local development (DEBUG ONLY) -->
    <!-- 10.0.2.2 is Android emulator's alias for host machine localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain>10.0.2.2</domain>
        <domain>localhost</domain>
        <domain>127.0.0.1</domain>
    </domain-config>

    <!-- Debug configuration for localhost testing -->
    <debug-overrides>
        <trust-anchors>
            <!-- Allow user-installed certificates for debugging (e.g., Charles Proxy) -->
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
