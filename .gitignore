*.iml
.gradle
/local.properties
.idea/
.DS_Store
**/build
/captures
.externalNativeBuild
.cxx
local.properties

# Keystore credentials (never commit)
*.keystore
keystore.properties

# Firebase config (contains API keys)
app/google-services.json

# Firebase emulator data and logs (local development)
emulator-data/
*-debug.log

# AI assistant instructions (verbose, not for repo)
CLAUDE.local.md
